# Dependencies & build output (rebuilt inside the image)
node_modules
dist
dist-ssr

# VCS & CI
.git
.gitignore
.github

# Local env / editor
*.local
.vscode
.idea
.DS_Store
*.swp

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Docs & meta not needed at build time
README.md
LICENSE

# K8s manifests are deployed separately, not baked into the image
k8s
